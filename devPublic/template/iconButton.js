/**
 * Created by huy on 5/20/17.
 */
const m = require('mithril')

const iconButton = {
    oninit: vnode => {
    },
    view: vnode => m('button',
        Object.assign(vnode.attrs, {

        }))
};

module.exports = iconButton